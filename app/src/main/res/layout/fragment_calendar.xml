<?xml version="1.0" encoding="utf-8"?>
<!--
    CalendarFragment 레이아웃

-->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="16dp"
        android:paddingBottom="24dp">

        <!-- 상단 타이틀 -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="캘린더"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="#222222"
            android:layout_marginBottom="16dp" />

        <!-- 월 이동 헤더 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="8dp">

            <ImageButton
                android:id="@+id/btn_prev_month"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:src="@android:drawable/ic_media_previous"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="이전 달" />

            <TextView
                android:id="@+id/tv_year_month"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="center"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="@color/primary_blue" />

            <ImageButton
                android:id="@+id/btn_next_month"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:src="@android:drawable/ic_media_next"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="다음 달" />

        </LinearLayout>

        <!-- 요일 헤더 + 날짜 그리드 카드 -->
        <com.google.android.material.card.MaterialCardView
            xmlns:app="http://schemas.android.com/apk/res-auto"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:cardCornerRadius="16dp"
            app:cardElevation="0dp"
            app:cardBackgroundColor="@color/light_blue"
            app:strokeColor="@color/stroke_gray"
            app:strokeWidth="1dp"
            android:layout_marginBottom="16dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="8dp">

                <!-- GridLayout: 첫 7개 자식은 요일 헤더 (XML 고정) -->
                <GridLayout
                    android:id="@+id/grid_calendar"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:columnCount="7"
                    android:useDefaultMargins="false">

                    <!-- 요일 헤더 (고정, Fragment 에서 제거하지 않음) -->
                    <TextView style="@style/CalendarDayHeader" android:text="일" android:textColor="#E53935"/>
                    <TextView style="@style/CalendarDayHeader" android:text="월"/>
                    <TextView style="@style/CalendarDayHeader" android:text="화"/>
                    <TextView style="@style/CalendarDayHeader" android:text="수"/>
                    <TextView style="@style/CalendarDayHeader" android:text="목"/>
                    <TextView style="@style/CalendarDayHeader" android:text="금"/>
                    <TextView style="@style/CalendarDayHeader" android:text="토" android:textColor="@color/primary_blue"/>

                    <!-- 날짜 셀은 CalendarFragment 에서 동적으로 추가됩니다 -->

                </GridLayout>
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- 선택된 날짜의 자격증 목록 (기본 GONE, 날짜 클릭 시 VISIBLE) -->
        <LinearLayout
            android:id="@+id/layout_cert_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone">

            <!-- 선택 날짜 텍스트 -->
            <TextView
                android:id="@+id/tv_selected_date"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="15sp"
                android:textStyle="bold"
                android:textColor="@color/primary_blue"
                android:layout_marginBottom="8dp" />

            <!-- 구분선 -->
            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="@color/stroke_gray"
                android:layout_marginBottom="8dp"/>

            <!-- 자격증 카드들은 CalendarFragment 에서 동적으로 추가됩니다 -->

        </LinearLayout>

    </LinearLayout>
</ScrollView>
